"""Delivery business module.

Use `cloudsite.modules.delivery.contracts.public` for the public boundary.
Importing the package itself must stay side-effect free so infrastructure models
can be registered without pulling application/contracts back through
`cloudsite.models`.
"""
