# CloudSite 建议立即处理的第一批事项开发文档

> 文档定位：CloudSite 2.0 Alpha 第一批架构收敛与基础设施建设任务  
> 目标版本：2.0.0 Alpha 后续迭代  
> 优先级：P0 / P1  
> 适用对象：项目维护者、AI 编程代理、后端/前端开发者、后续模块负责人  
> 前置条件：公开仓库清理与版本治理 PR 合并后开始执行  
> 原则：先收敛架构边界，再继续堆叠功能；先打地基，再扩展 Automation / AI / Provider 能力。

---

## 1. 文档目的

CloudSite 当前已经从“基于 AList 的资源展示站”发展成一个包含资源身份、目录索引、Catalog、分享、用户体系、自动化、AI、云下载、通知、权限、插件等能力的资源平台。

当前最需要解决的问题已经不是“缺少功能”，而是：

1. 业务能力增长速度已经超过当前模块边界的承载能力。
2. 1.x 的同步、调度、数据库与 API 进程内任务机制仍然支撑着 2.0 Alpha。
3. `models.py`、`migrations.py`、`rolling.py`、`globals.css` 等文件已经成为明显热点。
4. Automation / AI / Provider 扩展如果继续直接叠加，会迅速放大任务调度、数据库写入、失败恢复、审计和并发问题。
5. 当前代码已经出现“产品能力进入 2.0，但底层运行架构仍处于 1.x → 2.0 迁移阶段”的状态。

因此，第一批开发工作的目标不是继续扩功能，而是建立 2.0 后续开发必须依赖的基础框架。

本批次完成后，应达到以下状态：

- 模块依赖方向明确。
- 后台任务不再继续依赖 API 进程中的临时调度方式扩展。
- 新版索引机制可以独立演进。
- 数据访问开始脱离“任何模块都直接碰共享 Session/ORM”的模式。
- 自动化、AI、同步、元数据补全等任务拥有统一的任务模型。
- 关键用户路径存在端到端回归测试。
- 后续开发者或 AI Agent 可以只读取模块文档与契约，而不是重新理解整个项目。

---

# 2. 第一批事项总览

建议第一批只处理以下 6 个工作流。

| 编号 | 事项 | 优先级 | 是否阻塞后续开发 |
|---|---|---:|---|
| B1 | 冻结 2.0 模块边界与依赖规则 | P0 | 是 |
| B2 | 建立统一 Task / Worker / Lease 基础设施 | P0 | 是 |
| B3 | 建立新版 Indexing 模块骨架并隔离 Legacy Rolling | P0 | 是 |
| B4 | 建立 Repository / 数据访问边界 | P0 | 是 |
| B5 | 建立 Critical Path E2E 测试 | P1 | 强烈建议 |
| B6 | 建立模块级文档、Context Pack 与架构守卫 | P1 | 强烈建议 |

暂时不要在本批次中大规模处理：

- 新增 AI 能力。
- 新增复杂 Automation Rule。
- 新增大量 Provider。
- 微服务拆分。
- Redis / Kafka / Celery 引入。
- 全量 PostgreSQL 强制迁移。
- UI 大改版。
- 搜索引擎整体替换。
- 全面重写后端。

---

# 3. B1：冻结 2.0 模块边界与依赖规则

## 3.1 目标

建立正式的模块边界，使 CloudSite 从“目录分组”进入真正的模块化单体（Modular Monolith）。

目标架构：

```text
apps/api/cloudsite/
├── app/
│   ├── api.py
│   ├── lifecycle.py
│   └── composition.py
│
├── platform/
│   ├── db/
│   ├── http/
│   ├── security/
│   ├── tasks/
│   ├── observability/
│   └── settings/
│
├── modules/
│   ├── identity/
│   ├── users/
│   ├── providers/
│   ├── resources/
│   ├── indexing/
│   ├── search/
│   ├── catalog/
│   ├── collections/
│   ├── shares/
│   ├── submissions/
│   ├── notifications/
│   ├── automation/
│   └── delivery/
│
└── plugins/
```

注意：

第一批不要求把所有旧代码立即移动到上述目录。

本阶段的重点是：

- 定义未来目标边界。
- 明确哪些新代码必须进入新模块结构。
- 明确旧代码迁移策略。
- 防止继续增加新的共享大文件。

---

## 3.2 每个模块必须拥有的内容

目标模块结构：

```text
modules/catalog/
├── domain/
│   ├── entities.py
│   ├── value_objects.py
│   ├── policies.py
│   └── errors.py
│
├── application/
│   ├── commands.py
│   ├── queries.py
│   └── services.py
│
├── infrastructure/
│   ├── repository.py
│   └── models.py
│
├── api/
│   ├── router.py
│   └── schemas.py
│
├── contracts/
│   └── public.py
│
├── tests/
│   ├── test_domain.py
│   ├── test_application.py
│   └── test_api.py
│
└── README.md
```

并不要求每个模块一开始都具备所有文件。

但所有新模块至少必须具备：

- `README.md`
- `api/`
- `application/`
- `infrastructure/`
- `tests/`

---

## 3.3 模块依赖原则

建议正式写入架构规则：

### Rule 1

模块不得直接 import 其他模块的内部实现。

禁止：

```python
from cloudsite.modules.catalog.infrastructure.models import CatalogEntry
```

允许：

```python
from cloudsite.modules.catalog.contracts.public import CatalogReader
```

---

### Rule 2

API Router 不负责核心业务逻辑。

Router 只负责：

1. 请求解析
2. 权限入口
3. 调用 application service
4. 返回 response

禁止 Router 中继续增加：

- 多步骤 SQL
- 跨模块事务拼装
- 状态机逻辑
- 重试逻辑
- Provider 调用策略
- 索引策略

---

### Rule 3

模块不能依赖 `cloudsite.main`

当前部分任务、Middleware、测试兼容代码仍会访问：

```text
cloudsite.main
```

该方式在迁移期可以保留，但：

**禁止任何新模块新增对 `cloudsite.main` 的依赖。**

最终目标：

```text
main.py
```

只负责：

- 创建 FastAPI
- 注册 Router
- 注册 Middleware
- 生命周期
- 依赖注入
- 启动必要平台服务

---

### Rule 4

平台层不能依赖业务模块。

允许：

```text
modules -> platform
```

禁止：

```text
platform -> catalog
platform -> shares
platform -> automation
```

---

## 3.4 本事项需要新增的文档

建议创建：

```text
docs/
├── architecture.md
└── development/
    ├── module-boundaries.md
    ├── dependency-rules.md
    └── migration-strategy.md
```

如果不希望公开内部设计，可以放入项目维护者私有开发资料中。

公开仓库至少保留一份简化版 Architecture 文档。

---

## 3.5 验收标准

B1 完成必须满足：

- [ ] 有正式模块列表。
- [ ] 每个模块有 Owner / Responsibility。
- [ ] 明确模块公共 Contract。
- [ ] 明确禁止依赖方向。
- [ ] 新模块不得依赖 `cloudsite.main`。
- [ ] Router 不再允许新增复杂 SQL。
- [ ] CI 至少加入基础 Architecture Test。
- [ ] 所有新功能 PR 必须声明所属模块。

---

# 4. B2：建立统一 Task / Worker / Lease 基础设施

## 4.1 为什么必须第一批处理

CloudSite 后续大量功能本质都是后台任务：

- 索引扫描
- Metadata 提取
- Catalog 补全
- AI 内容生成
- Automation 执行
- 云下载
- 健康检测
- 资源重新验证
- Provider 同步
- 缩略图 / 文档预处理
- 后台清理任务

如果每个模块自行：

- `asyncio.create_task`
- 自己写 retry
- 自己写状态
- 自己做 crash recovery
- 自己做 timeout
- 自己做 concurrency limit

项目后续会快速失控。

因此必须尽快统一任务模型。

---

## 4.2 建议任务状态机

第一版建议：

```text
pending
   ↓
leased
   ↓
running
   ↓
succeeded

running
   ├──> retry_wait
   │       ↓
   │     pending
   │
   ├──> failed
   │
   └──> cancelled
```

还应支持：

```text
blocked
```

用于：

- 等待依赖任务
- 等待 Provider
- 等待用户确认
- 等待资源解锁

---

## 4.3 Task 数据模型

建议至少：

```text
task_id
task_type
queue
status
priority

payload_json

created_at
scheduled_at
started_at
finished_at

lease_owner
lease_expires_at

attempt_count
max_attempts

retry_at

dedupe_key

parent_task_id
root_task_id

last_error_code
last_error_message

result_json
```

建议未来支持：

```text
trace_id
resource_id
provider_id
user_id
```

但可作为第二阶段字段。

---

## 4.4 Lease 机制

Worker 获取任务时必须：

1. 原子领取。
2. 设置 `lease_owner`。
3. 设置 `lease_expires_at`。
4. Worker 周期 heartbeat。
5. Worker 崩溃后 Lease 过期。
6. 任务可重新领取。

目标：

即使 API / Worker 被 kill：

```text
Task 不会永久卡死。
```

---

## 4.5 Retry 机制

建议默认：

```text
attempt 1: 立即
attempt 2: 30 秒
attempt 3: 2 分钟
attempt 4: 10 分钟
attempt 5: 30 分钟
```

必须支持错误分类：

```text
RetryableError
PermanentError
RateLimitError
DependencyError
CancelledError
```

Provider 返回限流时：

```text
retry_at = provider_retry_after
```

而不是普通 exponential backoff。

---

## 4.6 Task Type 注册方式

禁止：

```python
if task.type == "xxx":
```

无限增长。

建议：

```python
registry.register(
    "index.scan.category",
    handler=ScanCategoryHandler(),
)
```

类似：

```text
index.scan.category
index.inspect.resource
metadata.extract
automation.execute
ai.generate.metadata
delivery.prepare
```

---

## 4.7 Worker

建议未来运行形态：

```text
cloudsite-api
cloudsite-worker
```

两者使用同一个 Python package。

Worker：

```bash
python -m cloudsite.worker
```

第一阶段甚至可以：

```text
api + embedded worker
```

但必须保证 Worker API 已经独立。

即：

```python
Worker.run()
```

不能依赖 FastAPI Request / App。

---

## 4.8 不建议第一版引入 Celery

原因：

当前规模尚不需要。

第一版建议：

```text
DB-backed queue
+
lease
+
worker loop
```

优势：

- 部署简单
- 可审计
- 无 Redis 强依赖
- 易备份
- 适合自托管

以后真正遇到吞吐瓶颈再替换 Transport。

---

## 4.9 Task API

管理员至少应该可以查看：

```http
GET /api/admin/tasks
GET /api/admin/tasks/{id}
POST /api/admin/tasks/{id}/retry
POST /api/admin/tasks/{id}/cancel
```

可选：

```http
POST /api/admin/tasks/{id}/requeue
```

---

## 4.10 测试要求

必须测试：

### 状态机

- pending → leased
- leased → running
- running → succeeded
- running → failed
- running → retry_wait
- retry_wait → pending

### Lease

- 正常续租
- Worker crash
- Lease 超时
- 第二 Worker 接管

### Retry

- Retryable
- Permanent
- RateLimit
- max attempts

### 幂等

同一个：

```text
dedupe_key
```

不能重复生成多个活动任务。

---

## 4.11 验收标准

- [ ] Task 有独立 ORM / Repository。
- [ ] Worker 不依赖 FastAPI。
- [ ] 支持 Lease。
- [ ] 支持 Retry。
- [ ] 支持 Dedupe。
- [ ] 支持优先级。
- [ ] 支持 Graceful Shutdown。
- [ ] Worker crash 后任务可恢复。
- [ ] 至少一个现有后台任务迁移到新 Task。
- [ ] 新 Automation / AI 任务禁止绕过 Task 系统。

---

# 5. B3：建立新版 Indexing 模块并隔离 Legacy Rolling

## 5.1 当前问题

Generic AList 本身不具备真正：

```text
delta
change cursor
change journal
webhook
```

所以当前系统需要 Rolling Full Verification。

这个机制对于 1.0 是合理的。

但随着资源数达到：

```text
20,000
50,000
100,000
```

持续递归扫描每个资源内部会越来越昂贵。

新版设计应该围绕：

```text
Inventory Scan
+
Resource Inspection
```

拆开。

---

## 5.2 新模型

### Category Inventory Scan

只扫描：

```text
Content Root
→ Category
→ Leaf Category
→ Resource Folder
```

不立即进入：

```text
Resource Folder 内部所有文件
```

目标复杂度：

```text
O(category + resource folder)
```

而不是：

```text
O(all nested files)
```

---

## 5.3 Resource Inspection

只有以下资源进入详情扫描：

1. 新资源
2. suspected changed
3. 用户手动提交
4. stale validation
5. manifest changed
6. provider identity changed
7. 管理员手工强制刷新

---

## 5.4 Resource Skeleton

Inventory 发现一个资源目录时：

应立即创建：

```text
Resource Skeleton
```

至少：

```text
resource_id
provider_id
object_id / path
name
category
status
discovered_at
last_seen_at
inspection_status
```

这样即使详情处理尚未完成：

```text
搜索 / 浏览仍能看到资源。
```

---

## 5.5 Manifest

优先支持：

```text
_cloudsite.resource.json
```

例如：

```json
{
  "id": "uuid",
  "title": "Example",
  "version": "1.0",
  "description": "...",
  "cover": "cover.jpg",
  "main_file": "example.zip",
  "tags": ["tool", "windows"]
}
```

资源详情优先级：

```text
manifest
>
category aggregate manifest
>
automatic detection
>
heuristic inference
```

---

## 5.6 Category Snapshot

关键安全原则：

一个 Category Scan 必须生成完整 Snapshot。

只有：

```text
pagination complete = true
```

才允许正式写：

```text
seen / missing / removed
```

如果 Provider：

- timeout
- pagination incomplete
- permission error
- unstable response

必须：

```text
ZERO OFFICIAL WRITE
```

不能误判资源删除。

---

## 5.7 Legacy Rolling 隔离

旧代码可以继续存在：

```text
sync/rolling.py
```

但必须变成：

```text
legacy compatibility path
```

禁止继续在它里面增加 2.0 新能力。

新代码进入：

```text
modules/indexing/
```

建议：

```text
indexing/
├── domain/
│   ├── snapshot.py
│   ├── inspection.py
│   └── change.py
├── application/
│   ├── scan_category.py
│   ├── inspect_resource.py
│   └── reconcile.py
├── infrastructure/
│   ├── provider_adapter.py
│   └── repository.py
└── tests/
```

---

## 5.8 Provider Capability

新 Indexer 必须依据 Provider capability 决策。

例如：

```python
if provider.capabilities.change_cursor:
    strategy = DeltaStrategy()
elif provider.capabilities.stable_object_id:
    strategy = InventoryStrategy()
else:
    strategy = ConservativeInventoryStrategy()
```

而不是：

```text
所有 Provider 一个同步算法。
```

---

## 5.9 与 Task 系统结合

所有扫描必须通过 Task：

```text
index.inventory.scan
index.resource.inspect
index.resource.reconcile
```

禁止：

```text
HTTP 请求触发后直接执行长扫描。
```

---

## 5.10 验收标准

- [ ] 新 Indexing 模块独立存在。
- [ ] Legacy Rolling 不再增加新 2.0 逻辑。
- [ ] Category Scan 与 Resource Inspection 分离。
- [ ] 支持完整 Snapshot。
- [ ] 分页不完整时不产生删除判断。
- [ ] 新资源可以先生成 Skeleton。
- [ ] Detail Inspection 可以异步补全。
- [ ] Provider Capability 驱动策略。
- [ ] 所有长任务走 Task Worker。
- [ ] 可支持 20k 资源基准测试。

---

# 6. B4：建立 Repository / 数据访问边界

## 6.1 当前风险

当前多个模块可能直接使用：

```python
StateSession
IndexSession
select(...)
session.execute(...)
```

如果继续扩展：

- 模块耦合数据库结构
- PostgreSQL 迁移困难
- Transaction 边界混乱
- 测试必须理解大量 ORM
- 后续多数据库支持困难

---

## 6.2 第一阶段目标

不是马上全面 Repository 化。

而是：

**所有新 2.0 模块必须通过 Repository 访问数据。**

例如：

```python
class TaskRepository(Protocol):

    async def create(...):
        ...

    async def lease_next(...):
        ...

    async def save(...):
        ...
```

---

## 6.3 Transaction Boundary

建议 Application Service 控制事务。

例如：

```python
async with uow:
    task = await repo.get(...)
    task.complete(...)
    await repo.save(task)
    await uow.commit()
```

Router 不做：

```python
await session.commit()
```

---

## 6.4 Unit of Work

第一版建议提供：

```python
class UnitOfWork:
    state
    index

    async def commit()
    async def rollback()
```

但注意：

SQLite 两个 DB：

```text
state.db
index.db
```

无法天然形成单个 ACID transaction。

因此跨库操作必须：

- 明确 eventual consistency。
- 或使用 outbox/task 补偿。
- 不要假装存在跨数据库事务。

---

## 6.5 PostgreSQL 兼容路线

本批不要求 PostgreSQL 上线。

但必须避免写出：

```text
SQLite-only domain logic
```

SQLite 特有行为应封装在：

```text
platform/db/sqlite/
```

未来：

```text
platform/db/postgres/
```

---

## 6.6 模型拆分策略

暂时不要一次性拆掉 `models.py`。

建议：

### Phase 1

新模块：

```text
自己的 models.py
```

### Phase 2

迁移旧模型。

### Phase 3

共享 `models.py` 只保留 Legacy。

---

## 6.7 Migration

同样：

不要继续无限增大：

```text
migrations.py
```

建议新结构：

```text
platform/db/migrations/
├── registry.py
├── 0021_tasks.py
├── 0022_indexing.py
└── ...
```

---

## 6.8 验收标准

- [ ] Task 模块完全通过 Repository。
- [ ] Indexing 新模块完全通过 Repository。
- [ ] 新 Router 不直接 commit。
- [ ] 新代码不从全局 models.py 随意 import。
- [ ] Migration 支持分文件注册。
- [ ] SQLite 专有行为开始隔离。
- [ ] PostgreSQL 不要求上线，但新领域代码不能假设 SQLite。

---

# 7. B5：Critical Path E2E

## 7.1 为什么需要

目前：

```text
Unit Test
API Test
CI
Docker Smoke
```

已经较强。

但仍缺：

```text
Browser Critical Path
```

如果未来开始大规模模块迁移，没有 E2E 很容易：

```text
后端测试全绿
前端测试全绿
真实用户流程坏了
```

---

## 7.2 建议 Playwright

建立：

```text
apps/web/e2e/
```

第一批只覆盖关键路径。

---

## 7.3 Critical Path

建议顺序：

### E2E-01 Setup

```text
首次启动
→ Setup
→ 配置 Provider
→ 首页可访问
```

### E2E-02 Authentication

```text
register
→ login
→ logout
→ login
```

### E2E-03 Browse

```text
首页
→ 分类
→ 资源
→ 详情
```

### E2E-04 Search

```text
搜索
→ 筛选
→ 打开结果
```

### E2E-05 Download

```text
资源详情
→ 下载
→ CloudSite Authorization
→ redirect
```

不要真的下载大文件。

验证：

```text
302 / redirect chain
```

即可。

### E2E-06 Share

```text
创建分享
→ 打开公开分享
→ 输入提取码
→ 下载
```

### E2E-07 Admin

```text
admin login
→ user
→ content root
→ diagnostics
```

### E2E-08 Catalog

```text
catalog list
→ detail
→ asset
```

### E2E-09 Submission

```text
用户提交
→ admin review
```

---

## 7.4 CI 策略

Pull Request：

```text
critical e2e
```

Release：

```text
full e2e
```

---

## 7.5 验收标准

- [ ] Playwright 加入项目。
- [ ] 至少 6 个 Critical Flow。
- [ ] PR 自动执行。
- [ ] 测试数据自动初始化。
- [ ] 测试结束自动清理。
- [ ] Screenshot / Trace 在失败时作为 CI Artifact。

---

# 8. B6：模块文档与 AI Context Pack

## 8.1 目标

CloudSite 已经足够大。

以后继续使用 AI 开发时：

```text
不能每次让 AI 重新读完整仓库。
```

必须建立模块级 Context。

---

## 8.2 每个模块 README

建议：

```markdown
# Catalog Module

## Responsibility

## Public API

## Domain Model

## Database Tables

## Dependencies

## Events / Tasks

## Security

## Failure Modes

## Tests

## Do Not

## Current Migration Status
```

---

## 8.3 AI Context Pack

建议维护：

```text
docs/context/
```

如果不想公开，可维护在私有开发资料。

建议：

```text
00-project-overview.md
01-architecture-rules.md
02-data-model-map.md
03-api-contract-map.md
04-task-system.md
05-indexing.md

modules/
  catalog.md
  indexing.md
  identity.md
  shares.md
```

---

## 8.4 每个 Context Pack 要控制长度

目标：

```text
每个模块 2k - 8k tokens
```

不要：

```text
单文件 50k tokens
```

AI Agent 应该可以：

```text
读取项目总览
+
当前模块
+
相关 Contract
```

完成任务。

---

## 8.5 ADR

建议新增：

```text
docs/adr/
```

例如：

```text
ADR-001-modular-monolith.md
ADR-002-task-queue.md
ADR-003-indexing-strategy.md
ADR-004-database-boundary.md
ADR-005-provider-capabilities.md
```

ADR 只记录：

```text
为什么做这个决定
替代方案
最终决定
后果
```

---

## 8.6 Architecture Test

建议 CI 增加简单规则：

禁止：

```text
modules/* -> cloudsite.main
platform -> modules
```

未来可以用：

- import-linter
- 自研 AST 检查
- pytest architecture test

---

# 9. 第一批建议开发顺序

建议严格按以下顺序：

```text
Step 1
B1 模块边界

Step 2
B4 Repository 基础接口

Step 3
B2 Task / Worker

Step 4
B3 Indexing v2

Step 5
B5 E2E

Step 6
B6 Context Pack / ADR / Architecture Guard
```

原因：

```text
模块边界
    ↓
数据访问边界
    ↓
任务基础设施
    ↓
新版索引
    ↓
E2E 保驾护航
    ↓
长期 AI/团队开发治理
```

---

# 10. 可并行开发方案

当 B1 完成后，可以拆成三条线并行。

## Track A：Platform

负责：

```text
Repository
UnitOfWork
Task
Worker
Lease
Retry
Migration
```

---

## Track B：Indexing

负责：

```text
Inventory
Snapshot
Skeleton
Inspection
Provider Strategy
Reconcile
```

依赖：

```text
Task Contract
Repository Contract
```

可以先使用 Fake。

---

## Track C：Quality

负责：

```text
Playwright
Architecture Tests
Context Pack
ADR
CI
```

---

# 11. 第一批明确禁止事项

第一批期间建议设置 Feature Freeze。

禁止大规模新增：

```text
AI Provider
Automation Rule
Catalog 大功能
社交系统
复杂权限
新下载协议
大型 UI 重构
```

Bug Fix 不受限制。

原因：

第一批本质是：

```text
降低后续开发复杂度。
```

如果一边拆基础，一边继续扩功能：

```text
迁移目标会持续移动。
```

---

# 12. 性能基准

新版 Indexing 开始前建议创建固定 Benchmark Fixture。

至少：

```text
1k
10k
20k
100k
```

资源结构。

测试：

```text
Inventory Scan 时间
Resource Inspection 时间
DB Write
Search Projection
Memory
Provider Request Count
```

核心指标不是：

```text
CPU 多快
```

而是：

```text
Provider Requests / 资源数
```

因为远端 API 才是真正瓶颈。

---

# 13. Observability 最低要求

Task / Indexing 必须带：

```text
task_id
task_type
resource_id
provider_id
duration
attempt
error_code
```

日志建议 JSON Structured Logging。

至少可以回答：

```text
为什么这个资源没索引？
哪个 Worker 处理？
失败几次？
Provider 返回什么错误？
什么时候重试？
```

---

# 14. 安全要求

Task Payload 中禁止保存：

```text
明文密码
Provider Token
Access Token
Secret Key
```

Task 只能保存：

```text
credential reference id
provider id
resource id
```

真正 Secret 通过 Security/Credential Service 获取。

---

# 15. 数据迁移原则

第一批不得：

```text
为了“架构漂亮”做大爆炸迁移。
```

使用：

```text
Strangler Migration
```

即：

```text
旧系统继续运行
新模块承接新路径
逐步迁移
最后删除 Legacy
```

例如：

```text
Legacy Rolling
     ↓
New Inventory
     ↓
部分 Provider
     ↓
全部 Provider
     ↓
Legacy Freeze
     ↓
删除
```

---

# 16. Definition of Done

本批次整体完成必须满足：

## Architecture

- [ ] 新模块目录已建立。
- [ ] Dependency Rules 已写入。
- [ ] Architecture Test 生效。

## Data

- [ ] Repository Pattern 已用于 Task。
- [ ] Repository Pattern 已用于 New Indexing。
- [ ] Migration 可以分文件维护。

## Task

- [ ] Task 状态机稳定。
- [ ] Lease 稳定。
- [ ] Retry 稳定。
- [ ] Worker crash 可恢复。
- [ ] Dedupe 生效。

## Index

- [ ] Inventory Scan 可运行。
- [ ] Resource Skeleton 可创建。
- [ ] Inspection 可异步执行。
- [ ] Snapshot 不完整时零删除写入。

## Quality

- [ ] Critical E2E 生效。
- [ ] CI 全绿。
- [ ] Backup / Restore 不受影响。
- [ ] Version Gate 不受影响。

## Documentation

- [ ] 模块 README 可供新开发者使用。
- [ ] ADR 建立。
- [ ] AI Agent 不需要读取全仓即可开发单模块。

---

# 17. 第一批建议 Issue 拆分

建议创建以下 Issues：

```text
ARCH-001 Define 2.0 module boundaries
ARCH-002 Add architecture dependency tests

DB-001 Introduce repository interfaces
DB-002 Split migration registry

TASK-001 Define task domain model
TASK-002 Implement DB task repository
TASK-003 Implement lease and heartbeat
TASK-004 Implement retry policy
TASK-005 Implement worker runtime
TASK-006 Add task admin API

INDEX-001 Create indexing v2 module
INDEX-002 Implement category inventory snapshot
INDEX-003 Implement resource skeleton
INDEX-004 Implement resource inspection
INDEX-005 Implement reconcile logic
INDEX-006 Integrate provider capabilities
INDEX-007 Migrate first provider path

E2E-001 Add Playwright infrastructure
E2E-002 Add auth flow
E2E-003 Add browse/search flow
E2E-004 Add share/download flow
E2E-005 Add admin/catalog flow

DOC-001 Add module README standard
DOC-002 Add ADR structure
DOC-003 Add AI context pack
```

---

# 18. 推荐首个 Vertical Slice

第一批不要同时铺开所有 Task + Indexing 功能。

建议首个完整 Vertical Slice：

```text
管理员触发 Category Scan
        ↓
创建 Task
        ↓
Worker Lease
        ↓
Provider List
        ↓
生成 Inventory Snapshot
        ↓
发现新 Resource Folder
        ↓
创建 Resource Skeleton
        ↓
创建 Inspect Task
        ↓
Worker Inspection
        ↓
Resource Detail 更新
        ↓
搜索可见
```

如果这条链路跑通：

基本意味着：

```text
Task
Worker
Repository
Indexing
Provider
Resource
Search
```

核心架构已经完成第一次真实集成。

---

# 19. 第一批完成后再启动的第二批事项

完成本批后，再考虑：

1. Automation 全量迁移 Task。
2. AI Job 全量迁移 Task。
3. Cloud Download Task 化。
4. PostgreSQL Adapter。
5. 独立 Worker Compose。
6. Provider 插件增强。
7. Frontend CSS 模块化。
8. Catalog 模块迁移。
9. Notification / Submission 模块迁移。
10. Legacy Rolling 删除计划。

---

# 20. 最终目标

本批工作的成功标准不是：

```text
代码更多。
```

而是：

```text
以后新增功能的成本下降。
```

完成以后，一个新能力应该可以：

```text
确定所属模块
→ 定义 Contract
→ 创建 Task
→ 通过 Repository 持久化
→ Worker 执行
→ E2E 验证
```

而不是：

```text
改 main.py
→ 改 models.py
→ 改 migrations.py
→ 改 rolling.py
→ 改 router
→ 到处 import Session
→ 希望测试没坏
```

CloudSite 2.0 的第一阶段真正目标，应当是把项目从：

> 功能快速增长的单体

推进到：

> 边界明确、可持续演进的模块化资源平台。

只有完成这一层，后续 Automation、AI、Provider、Catalog、团队协作等能力继续增长时，项目复杂度才不会成倍失控。
